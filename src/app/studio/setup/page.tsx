import { SetupWizard } from '@/components/setup'

export const metadata = {
  title: 'Setup | Talk Show Go',
  description: 'Set up Talk Show Go for the first time',
}

export default function SetupPage() {
  return <SetupWizard />
}
