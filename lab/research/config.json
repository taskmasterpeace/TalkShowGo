{
  "schema_version": 1,
  "youtube": {
    "trusted_channels_max": 5,
    "results_per_trusted_channel": 2,
    "global_results": 6,
    "transcript_limit": 6,
    "transcript_words_per_video": 12000,
    "transcript_words_total": 60000,
    "cache_ttl_hours": 168
  },
  "web": {
    "default_provider": "searxng",
    "searxng": true,
    "searxng_url": "http://localhost:8888",
    "local_llm_url": "",
    "local_llm_model": "qwen3.5",
    "searxng_note": "FREE web supplement (the Perplexica substance). searxng=true tries the self-hosted SearXNG (docker compose up -d searxng, :8888) FIRST at $0, then falls back to the paid OpenRouter/Perplexity providers below only if it's down or empty. local_llm_url is OPTIONAL neutral synthesis on a free OpenAI-compatible box (Mac Mini Ollama http://192.168.1.217:11434 or cupcake) — blank means use the raw snippet digest (still free, zero LLM dependency). Set searxng=false to force the paid path.",
    "max_results": 6,
    "openrouter_model": "google/gemini-2.5-flash-lite",
    "perplexity_model": "perplexity/sonar"
  },
  "parser": {
    "provider": "local + openrouter",
    "local_url": "http://192.168.1.217:11434",
    "local_model": "qwen3.5",
    "model": "google/gemini-2.5-flash-lite",
    "temperature": 0.1,
    "max_output_tokens": 5000,
    "parser_note": "FREE-first evidence mining. 'local + openrouter' tries the local Ollama box (local_url = Mac Mini, GPU-independent; cupcake http://192.168.1.249:11434 is the alt) at $0, then falls back to the cheap cloud model only if the box is OFF. A 3s liveness probe means an off box fails fast instead of hanging. Set provider to 'openrouter' to force cloud, or 'openrouter + local' to prefer cloud with a local fallback."
  },
  "impartiality": {
    "min_distinct_publishers": 2,
    "require_evidence_on_factual_moves": true,
    "reject_invented_urls": true,
    "excluded_publishers": ["letstalkbattlerap", "let's talk battle rap", "lets talk battle rap", "ltbr"],
    "excluded_note": "Robert's standing rule: never name/surface these outlets. In a cite-everything architecture there is no mine-silently path (a claim either shows the source name or is uncited), so we simply do not source from them. Matched case-insensitively against a source's url + publisher + title. Add any outlet Robert flags as disrespectful."
  }
}
