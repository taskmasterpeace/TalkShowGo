# TalkShowGo — Self-Improving Loop (started 2026-09-04)

Robert's mandate (`/loop 30m`): **obsess over a talk-show engine that works with SEGMENTS** — multi-story
episodes chained with smooth transitions — that **WOWS a producer**. Audio only (no video). **Polish +
efficiency.** Cheap models OK (gpt-4o-mini / gpt-4.1-mini) if they help cost/speed. **Robert can't listen —
judge quality by the TRANSCRIPT** (`segment_final.md`) + the blind Codex judge, never by ear.

## The cycle (run ONE per iteration, ~30 min)
1. Read this log; pick the next step from the roadmap (or the biggest weakness the last judge found).
2. Make ONE targeted change.
3. Verify: build a show (or re-floor an existing beatcard), READ `lab/shows/<slug>/floor/segment_final.md`,
   run the blind Codex judge (strip speaker labels → `scratchpad/blind_*.txt` → codex).
4. Append an entry: change · judge score (before→after) · verdict · next step.
5. If verified better (or neutral + clearly correct), `git -C /d/git/talkshowgo` commit it. Never commit a regression.

## Roadmap (in priority order)
> **KEY REFRAME (iter 6, backed by evidence):** the 3-5 quality plateau is an **EVIDENCE problem, not an engine
> problem.** The floor engine correctly argues whatever's in the ledger; a one-sided ledger (only roster FACTS,
> no controversy) yields a one-sided "debate" and a conceding against-host. Bigger models, punch-up, de-polish,
> and a word-chant guard all failed because none of them add ammunition. **The lever is RESEARCH: gather DEBATE
> MATERIAL (opinions, criticism, alternatives), not just facts.** New P1 below.
- **P1 (NEW) — Research gathers DEBATE MATERIAL.** Controversy/reaction angle-queries in the stringer + a parser
  that captures DISAGREEMENT, so both sides get real ammunition. Without this, "collision" is unwinnable. (iter 7)
- **P1-old — Collision quality (single segment).** First Take debaters must take OPPOSING sides (A argues yes, B
  argues no). Kill co-signing/agreement, metaphor-loops, empty turns, end-name tics. Verify: judge "heat" up.
  *Status: the ENGINE side is done (role-aware collision, guards); what remains is starved by one-sided evidence
  (see KEY REFRAME) — so it's blocked on P1-new, not on more engine work.*
- **P2 — SEGMENTS (the headline).** One episode = several story-segments (e.g. captains → QB → roster),
  each a mini-floor, stitched with a host-written transition ("Alright, that's the captains. Now the QB room…"),
  show intro on front + outro on back, leveled into one mp3. Generalize to any show's segment lineup.
- **P3 — Efficiency + polish.** Cheap models where they don't hurt the transcript (test gpt-4o-mini / 4.1-mini
  per seat, keep what the judge says is >= current). Faster pipeline, fewer wasted calls. Per-show cast/format
  auto-default so builds never grab the wrong lineup.

## Ops notes
- Dev server: `preview_start name=talkshowgo` (port 3000). It has died mid-session before — restart if curls refuse.
- Floor/brief run on OpenRouter; reasoning models (sonnet-5) need `reasoning:{effort:'low'}` (already wired in
  run_floor `call` + agent-brief `tryOR`) or they burn the token budget and return empty/malformed.
- Voice work needs cupcake gateway `running:false && queue_depth:0` (poll `:8700/v1/health`). Voices are text —
  Robert can't listen — so voicing is OPTIONAL for the loop; prioritize the TRANSCRIPT. Only voice a keeper.
- Key files: `run_floor.mjs` (the argument engine), `agent-brief.ts` (stances), `compile_beat.mjs` (seats +
  roles + stances), `render_breeze.mjs` (voice). Beats carry `show.hosts` (per-show cast) + `show.show_type`.
- Blind judge = the honesty check. Never grade your own writing; always route the transcript to Codex blind.

## Iterations
| # | change | judge (before→after) | verdict | next |
|---|--------|----------------------|---------|------|
| 1 | Forced "opposing sides on a yes/no" in the showrunner (compile_beat director) prompt | baseline: debaters fully agreed → PARTIAL | More tension, but the two DEBATERS (Big Mike+Andrew) still co-sign; the MODERATOR (Renee) ended up carrying the dissent. Root cause: the showrunner has no ROLE-awareness (mod vs A vs B). | iter 2 |
| 2 | Role-aware collision: showrunner (compile_beat director) now gets show_type + host lanes; for moderated-collision the moderator gets NO side and the two debaters split FOR vs AGAINST | 4 → **5** | WIN — real First Take now: Renee referees, Big Mike FOR vs Andrew AGAINST (blind judge: distinctness 7/10, moderator + opposing sides confirmed). Residual: moderator tilts toward the against-side; Big Mike's all-caps slogans repeat; the against-case is thin. | iter 3 |
| 3 | Built `make_episode.mjs` — the SEGMENT STITCHER: moderator COLD OPEN naming the slate + smooth TRANSITIONS between segments + SIGN-OFF, concatenated into one `episode.md` (render-ready). | mechanism verified (read the generated lines) | WIN — the seam that turns single-story cuts into a multi-story SHOW. Renee moderating: cold-open names both stories, transition is a real broadcast handoff, sign-off clean. **Limitation:** tested with 2 captains segments + topic-label overrides; a content-distinct run + voicing is next. | iter 4 |
| 4 | make_episode **trim**: strip each segment's OWN greeting + sign-off so the stitched episode has ONE cold open + one close (no per-segment "Good evening" resets). Built + blind-judged a REAL 2-segment Falcons episode (captains slate → Bijan). | single-seg **5** → episode **3** (two blind runs, both 3) | Trim VERIFIED correct (openers now start on substance, resets gone) but score didn't move: **CONTENT is the wall, not structure.** Judge, both runs: (a) the two segments read as the SAME story stretched — seg2 (Bijan) REUSED the captains dossier because the QB stringer returned **0 evidence**; (b) slogan/metaphor LOOP persists ("identity", "leadership comes in many forms", "Mm", wrestling metaphors); (c) NO evidence progression — debaters recycle, never cite a NEW [E##] or concede; (d) moderator tilts (A sides with C). | iter 5 |
| 5 | Room **slogan-chant guard** in `run_floor` badTurn: cap any non-subject 6+ letter word once the room has said it 3x (apostrophe-collapsed so "that's" can't trip it; exempts question/evidence/host-name tokens; universal, even unfiltered hosts). Verified on a fresh single-segment captains floor (seed 42). | 5 → **3** | **REVERTED — not a win.** The guard works MECHANICALLY (kills literal chanting; "that's" false-positive fixed via min-6/apostrophe-strip; 41→20 rejections) but the blind judge did NOT improve: **the wall is CONCEPTUAL, not lexical.** Third judge in a row converges: the AGAINST side (Andrew) has NO concrete case — concedes ("you're not wrong, great picks") then restates one vague thesis ("leadership comes in many forms") in fresh words, so no clash develops, no evidence progresses, and the moderator drifts ("Right." filler turns). Word-level de-dup can't fix an argument with no real opposition, and it adds retry latency (hurts P3). Reverted to keep the engine at the 5/10 baseline. | iter 6 |
| 6 | **ROOT-CAUSE diagnosis** (no code change — a diagnosis iteration that redirected the roadmap). Inspected the captains dossier `str_ilppbi7w`: all **8 evidence claims are one-sided ROSTER FACTS** ("the 7 captains are X", "announced by Stefanski", "Bijan & Deablo ARE captains") — ZERO controversy, snub, alternative, or criticism. Confirmed the `compile_beat` director ALREADY forces opposing sides + a neutral moderator; the failure is upstream. | n/a (diagnosis) | **THE PLATEAU IS AN EVIDENCE PROBLEM, NOT AN ENGINE PROBLEM.** The RESEARCH gathers FACTS, not DEBATE MATERIAL. "Did they pick the RIGHT captains?" is undebatable from evidence that only says WHO the captains are, so the against-host has no ammunition and concedes — exactly what 3 blind judges saw. No engine/director/word-guard change can manufacture a debate the sources never contained. I nearly changed the director prompt —**that would have been the wrong layer.** `runStringer` runs ONE neutral query (assignment.text + Q) → YouTube returns factual recaps → the impartial parser faithfully extracts facts. Nothing seeks the disagreement. | iter 7 |
| 7 | **RESEARCH now gathers DEBATE MATERIAL** (`src/lib/command/stringer.ts`): (a) a CONTROVERSY/REACTION search pass, interleaved with the neutral pass so reaction sources reach the transcribed top-N; (b) a RELEVANCE filter — keep only titles sharing a discriminating subject token (kills the AI-slop/foreign-fiction spam the reaction query attracts; only trims when >=3 survive so a thin topic isn't gutted); (c) `PARSE_SYS` now extracts EACH competing opinion as its OWN ATTRIBUTED_CLAIM receipt (was collapsing them into a context note the floor never sees). Gated by `cfg.stringer.controversy_pass`. | QB question **0 evidence (UN-BUILDABLE) → 34 valid (20 opinions); floor 4/10, argument-progression 2→5**, judge: "usable reporting underneath / more real substance". Captains: 8 facts → 33 (spam filtered). | **WIN — the research capability IS the lever.** A genuinely contestable question (QB room) that the OLD code could not build at all (0 evidence) now yields a rich two-sided dossier and a substantive floor. typecheck clean, no regression (captains still builds, now richer). **Remaining cap (floor 4, heat 2):** the DIRECTOR didn't split the two debaters onto OPPOSING sides of THIS question (both landed on "it's unsettled") — now FIXABLE because the evidence finally supports both a "yes set" and "no problem" case. | iter 8 |
| 8 | **DIRECTOR committed-verdict split** (`compile_beat`): on a yes/no the two debaters MUST commit to OPPOSING verdicts (YES vs NO) even if their honest lean is nuanced — hedging to "unsettled/complicated/too soon" is a FAILURE (nuance is the moderator's job); each debater holds the receipts backing THEIR verdict. Re-compiled + re-floored the QB beat (same evidence, seed 42). | QB floor **4 → 4 overall, but HEAT 2 → 5** (progression stable 5) | Directional WIN, committed. The split works — debaters now hold opposing verdicts (Mike=YES "they're set / not a problem" vs Andrew=NO "still unsettled") and the judge's HEAT jumped 2→5, no regression. Necessary complement to iter 7 (two-sided evidence is wasted if the director lets both hosts hedge). Overall still 4 because the split EXPOSED two separate issues: **(a) CASTING-FIT** — the hype-man persona (Big Mike) got the YES verdict and sold spectacle ("pay-per-view/main event") instead of arguing it ("the pro-set side never clearly argues that position"); **(b) MODERATOR** — Renee went passive (4x "Mm"/"Right") AND tilted to the NO side. | iter 9 |
| 9 | **FIXED THE FIRST-TAKE ROLES (3 edits, one coherent fix):** (a) `compile_beat` pre-chooses the neutral MODERATOR by lane (anchor/framing) and passes fixed roles to the director — names the moderator + assigns the OTHER TWO explicit opposing verdicts (YES vs NO) BY ID (model only writes the words, never picks sides); deterministic lock forces the anchor to "MODERATOR:" if the model still slips. (b) `run_floor`: the moderator never emits backchannels (a desk anchor going "Mm"/"Right" is the passive-filler knock). (c) **director model flash-lite → gpt-4.1-mini** — flash-lite kept collapsing both debaters onto the same side; gpt-4.1-mini reliably obeys the fixed roles (verified: Renee=MODERATOR, Mike=YES, Andrew=NO). | QB floor **4 → 5 overall; HEAT 4 → 7** (highest of the whole loop); moderator 3→4/5, 0 moderator filler | **WIN — the plateau broke.** With reliable roles the floor finally collides: judge "clear opposing positions and frequent direct rebuttals". Mike argues a real YES (veteran + upside + "off-ramp is luxury not doubt") vs Andrew's real NO (evaluation year + cap moves + 2027 off-ramp), Renee opens/drives neutrally and calls out Mike's dodge ("citing me to win an argument I never settled"). **Remaining caps:** (i) Big Mike's HYPE persona ("PYRO/MAIN EVENT") dents naturalness (casting-fit — iter 10); (ii) the moderator goes quiet mid-segment and tilts NO at the CLOSE (her exit line endorses a side). | iter 10 |
| 10 | **MODERATOR DRIVES** (`run_floor` pickNext): cut the neutral moderator in whenever they've been silent >=3 real turns — press the last speaker's weak point OR inject a NEW angle neither debater raised. (Also ran a CASTING experiment: swap hype-announcer Big Mike → analytical Cassius so both debaters are analysts.) | QB floor (same iter-9c beatcard) **5 → 5 overall; MODERATOR 4 → 5**. Casting experiment: **5 → 4, heat 7→5 (FAILED, not committed)** | Moderator-drive committed (correct + moderator axis up): the anchor now injects angles the debaters miss ("the offseason moves didn't make the roster better — does the QB room even matter?") and closes neutral. Overall flat 5 because two OTHER constraints now bind. **CASTING FAILED** (scratchpad only, not committed): two analytical debaters are calmer but LESS heated ("circles two talking points") — the hype-announcer actually COLLIDES HARDER; calm ≠ compelling, so don't swap the cast. **Binding constraints (judge, repeatedly):** (a) Big Mike's HYPE catchphrases ("WALKOUT MUSIC/MAIN EVENT" — "catchphrases substitute for rebuttals"); (b) OUTLET-NAME repetition ("Locked On Falcons says…" every line) — a NEW side-effect of iter-7 opinion-evidence: ATTRIBUTED_CLAIMs carry a source_name and hosts parrot it. | iter 11 |
| 11 | **NATURALNESS attempt (REVERTED):** (a) auto-pick attribution mode **D (REPORTED)** for opinion-heavy ledgers + strip outlet names from the receipts (kill the "Locked On Falcons says…" spam); (b) announcer-lane **nudge** (Big Mike's energy must CARRY an argument, not replace it). | QB floor **5 → 4** (mode-D alone: 4; mode-D + nudge: 4). Outlet mentions **8 → 0**, but naturalness stuck at **3**. | **REVERTED — not wins.** Mode-D killed the outlet spam (verified 0) but the judge now calls the generic attribution **"vague reporting"** (traded one tic for another); the nudge cut pure-hype but Big Mike just grew a NEW repetitive construction ("that's not X, it's Y" ×3). **Third naturalness stall in a row** (iter-10 casting, + both halves here). **LESSON: the 5→6 gap is NOT reachable by attribution-mode or per-host prompt tweaks** — the caps are DEEP: (1) Big Mike's persona is repetition-based (whack-a-mole: kill one slogan family, another emerges); (2) the moderator endorses a side at the CLOSE (named every run — 9c/10/11); (3) THIN evidence — "neither side develops or tests the evidence deeply" (the QB ledger has ~5 distinct facts, so both recycle). Reverted to the iter-10 5/10 baseline. | iter 12 |
| 12 | **SEGMENTS headline — built + judged a REAL 2-story episode, then sharpened the stitcher.** Built a genuine 2-segment Falcons episode (QB + NFC South, each its OWN fresh two-sided research) on the improved engine, then improved `make_episode`: content-aware **TRANSITIONS + SIGN-OFF** (feed the cheap model each segment's CLOSING line so it names the SPECIFIC takeaway, not a generic "that's a big question"; sign-off recaps BOTH stories + a forward hook) + mojibake/smart-quote cleanup. | Episode **5 → 5.5**: coherence 6→7, transitions/sign-off **5→7**, mojibake→0 (segment naturalness unchanged 3). | **WIN — the multi-segment FORMAT works + the stitcher is sharper.** Reads as one coherent show: cold open names both stories, the transition now nods to seg-1's real landing ("a launchpad for some, a question mark for others") before pivoting, Renee threads BOTH segments as moderator and closes with a specific two-story recap + a "next week: roster moves" hook; distinct topics (coherence 7). **Remaining episode-level caps:** (a) the two segments feel "optimist-vs-skeptic reskinned" — SAME cast postures across topics (Mike hype-YES, Andrew caution-NO) → needs per-segment role/pairing VARIETY; (b) segment naturalness still 3 (Big Mike all-caps + C's "Right/Mm" — the single-segment persona wall); (c) RELIABILITY: Big Mike's `deepseek-v3.2` engine TIMED OUT a brief (slow seat — a build-fragility + latency risk). | iter 13 |
| 13 | **Big Mike engine swap: `deepseek-v3.2-exp` → `openai/gpt-4.1-mini`** (added the DNA-registry entry to models.json so the brief resolves it; cast temp 0.95→0.85). deepseek was 45-265s/turn and timed out briefs. | QB floor **5 → 5 overall** (naturalness **3→4**, heat/moderator held); **latency: Big Mike turns 45-265s → ~1.5-2.5s (max 2466ms); brief 3/3 first try; floor ~30s vs ~5min.** | **WIN (reliability + efficiency — the P3 lever Robert named).** Big Mike's turns dropped ~20-100x, the slow timeout-prone seat is gone, the whole floor now builds in ~30s. Quality neutral-to-slightly-up (overall 5=5, naturalness 3→4, no heat/moderator loss): a fresh model didn't crack the naturalness wall (still "MAIN EVENT/OWN" slogans) but didn't hurt, and gpt-4.1-mini Big Mike engages sources directly ("those analysts watch MATCHUPS not PRACTICE FIELDS") + Renee's neutral close is sharp ("B's got the practice reps, C's got the surgery report, nobody's got the December box score"). **Finding:** brief flakiness is transient + spread across hosts (deepseek timeout, gemini "malformed stance") — the briefing/agent route should RETRY. | iter 14 |
| 14 | (next) **(A, recommended) Brief-retry in the `briefing/agent` route:** transient per-host failures (timeout, malformed JSON) sink a whole build — I hit 2/3 THREE times this fire and had to hand-retry. Add an in-route retry (2-3 attempts per host on transient errors) so a build is robust. Concrete reliability, P3. **(B) Per-segment VARIETY:** rotate which host argues which side per segment so a multi-story episode stops feeling "optimist-vs-skeptic reskinned" (the episode's #1 cap). **(C) the naturalness wall** (segment nat stuck 3-4) remains the hardest — deeper than prompt/model tweaks; revisit only with a new idea (e.g. richer/more-distinct evidence so sides stop recycling ~5 facts). | — | — | P3 efficiency |
